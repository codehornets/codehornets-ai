# CodeHornets AI Environment Configuration
# Copy this file to .env if you need to customize settings

# Authentication: Uses Claude Code web session (no API key needed)

# Optional: Node environment (default: production)
NODE_ENV=production

# Optional: Redis URL for coordination (default: redis://redis:6379)
REDIS_URL=redis://redis:6379

# Optional: Enable hooks mode (leave empty to disable hooks)
HOOKS_MODE=enabled

# Optional: Activation mode for workers (default: inotify)
# Options: inotify, polling
ACTIVATION_MODE=inotify

# Optional: Heartbeat interval in seconds (default: 10)
HEARTBEAT_INTERVAL=10

# Optional: Docker compose project name (default: codehornets-ai)
COMPOSE_PROJECT_NAME=codehornets-ai
