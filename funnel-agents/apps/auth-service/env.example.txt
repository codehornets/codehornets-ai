# Auth Service Configuration

# Service Ports
AUTH_SERVICE_PORT=3001
AUTH_SERVICE_TCP_PORT=3011
AUTH_SERVICE_HOST=0.0.0.0

# Node Environment
NODE_ENV=development

# Database Configuration
DATABASE_URL=postgresql://funnel_agents:secret@localhost:5432/funnel_agents

# JWT Configuration - REQUIRED FOR SERVICE TO START
# SECURITY WARNING: These secrets MUST be set. The service will fail to start without them.
# Generate secure random secrets using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# NEVER commit actual secrets to version control
# NEVER use these example values in production
JWT_SECRET=REPLACE_WITH_SECURE_RANDOM_STRING_MINIMUM_32_CHARACTERS
JWT_REFRESH_SECRET=REPLACE_WITH_DIFFERENT_SECURE_RANDOM_STRING_MINIMUM_32_CHARACTERS

# Email Configuration
# Choose one provider: SMTP or SendGrid

# SMTP Configuration (Option 1 - for most email providers)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-gmail-app-password
SMTP_FROM=noreply@funnelagents.ai

# Advanced SMTP Options (optional)
SMTP_REQUIRE_TLS=true
SMTP_REJECT_UNAUTHORIZED=true

# SendGrid Configuration (Option 2 - alternative to SMTP)
# SENDGRID_API_KEY=your-sendgrid-api-key-here

# Email Settings
EMAIL_FROM=noreply@funnelagents.ai
EMAIL_REPLY_TO=support@funnelagents.ai
SUPPORT_EMAIL=support@funnelagents.ai

# Frontend URL (for password reset and account links)
FRONTEND_URL=http://localhost:3000
DOCS_URL=https://docs.funnelagents.ai

# CORS Configuration
CORS_ORIGIN=http://localhost:3000
